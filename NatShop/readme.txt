Using SpringBoot framework which including JUnit 5 and Mockito.

Application will invite user to fill quantity of product (in command prompt).
The shop cart will be displayed in the prompt and saved into JSON file (NatShop.json) in the current project (not in C:\temp) because of denied access.

A JUnit test also generates a JSON file (NatShopTest.json)
